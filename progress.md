# Progress

> Add a line here for every change you make while working.
> When you're ready to push, just say "push" and the AI will use this to build the commit message and changelog.
> Prefix with `fix:`, `feat:`, `refactor:`, `style:`, `chore:` when possible (but not required).

## In progress

- feat: add push workflow prompt with progress.md integration
- feat: BubbleWrapper expands from anchor size on open
- fix: workspace_member insert missing id field (add ElioField.uuid())
- feat: add ElioField.uuid() type with auto-generation on insert
- fix: elioapi/frontend vite alias

---

## Pushed

